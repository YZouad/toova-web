/**
 * Previously asked GitHub Actions to rebuild static OG pages.
 * Link previews are served live by the toova-og-gateway Worker now.
 */
export async function requestUnfurlDeploy(): Promise<void> {
  return;
}
